using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FadeOutObject : MonoBehaviour
{/*
    public void fade(GameObject target, float speed, GameObject[] exceptions)
    {
        foreach(Transform child in target)
        {
            GameObject Object = child.gameObject;
            if(Object.GetComponent<SpriteRenderer>())
            {
                Object.GetComponent<SpriteRenderer>().color.a;
            }
        }
    }
    private void fadecolor(){}
    //UNFINISHED
    */
}
